#!/sbin/msh

onexit /etc/boot/failure

setenv PATH /bin:/sbin:/usr/bin

/sbin/kmount -vc /dev/pts
/sbin/kmount -vc /run
/sbin/kmount -vc /mnt
/sbin/kmount -vc /tmp
# remount root read-write
/sbin/kmount -e /

umask 0002

mkdir /run/ctrl

prctl no-new-privs

exec /sbin/service/svhub
